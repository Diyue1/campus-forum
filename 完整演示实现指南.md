# 🚀 完整演示实现指南

本指南将帮助你逐步实现所有需要外部服务的演示功能。

---

## 📋 实现清单

- [ ] 1. 设置 GitHub 仓库并推送代码触发 CI
- [ ] 2. 运行 Lighthouse 性能测试
- [ ] 3. 运行自定义性能测试
- [ ] 4. 运行 k6 负载测试
- [ ] 5. 设置并运行 SonarQube 分析
- [ ] 6. 运行完整的回归测试（包括 E2E）

---

## 1️⃣ 设置 GitHub 仓库并推送代码触发 CI

### 步骤 1.1：初始化 Git 仓库（如果还没有）

```bash
# 检查是否已经是 Git 仓库
git status

# 如果不是，初始化
git init
git add .
git commit -m "feat: 初始化项目"
```

### 步骤 1.2：在 GitHub 创建仓库

1. 访问 https://github.com/new
2. 创建新仓库，例如：`campus-forum`
3. **不要**初始化 README、.gitignore 或 license（我们已经有了）

### 步骤 1.3：连接远程仓库

```bash
# 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/campus-forum.git

# 或使用 SSH
git remote add origin git@github.com:你的用户名/campus-forum.git

# 推送代码
git branch -M main
git push -u origin main
```

### 步骤 1.4：验证 CI 触发

1. 推送后，访问 GitHub 仓库
2. 点击 "Actions" 标签
3. 查看 CI/CD Pipeline 是否自动运行

### 步骤 1.5：测试 CI 触发

```bash
# 修改一个文件
echo "# 测试 CI" >> README.md

# 提交并推送
git add README.md
git commit -m "docs: 测试 CI 触发"
git push

# 访问 GitHub Actions 查看运行结果
```

### 可能遇到的问题

**问题 1：GitHub Actions 失败**
- 检查 `.github/workflows/ci.yml` 配置
- 查看错误日志
- 确保所有依赖都在 `package.json` 中

**问题 2：推送失败**
- 检查 Git 凭据
- 确保有仓库写入权限
- 尝试使用 SSH 密钥

---

## 2️⃣ 运行 Lighthouse 性能测试

### 步骤 2.1：启动开发服务器

```bash
# 终端 1：启动开发服务器
npm run dev
```

等待服务器启动，确保显示：
```
➜  Local:   http://localhost:3000/
```

### 步骤 2.2：运行 Lighthouse 测试

```bash
# 终端 2：运行 Lighthouse
npm run test:lighthouse
```

### 步骤 2.3：查看报告

```bash
# 打开报告目录
explorer reports\lighthouse

# 或直接打开最新的 HTML 报告
```

### 预期输出

```
🚀 启动 Lighthouse 性能测试...

📊 Lighthouse 测试结果:

性能 (Performance):      85/100
可访问性 (Accessibility): 92/100
最佳实践 (Best Practices): 88/100
SEO:                      90/100

✅ 报告已保存到: reports/lighthouse

⏱️  关键性能指标:

首次内容绘制 (FCP):       1.2s
最大内容绘制 (LCP):       2.1s
总阻塞时间 (TBT):         150ms
累积布局偏移 (CLS):       0.05
速度指数 (Speed Index):   2.5s

✅ 性能测试通过！
```

---

## 3️⃣ 运行自定义性能测试

### 步骤 3.1：确保开发服务器运行

```bash
# 如果还没启动
npm run dev
```

### 步骤 3.2：运行性能测试

```bash
# 新终端
npm run test:performance
```

### 步骤 3.3：查看报告

```bash
explorer reports\performance
```

### 预期输出

```
🔍 测试 URL: http://localhost:3000

📊 测试 1: 页面加载性能...
   ⏱️  加载时间: 1523ms ✅

📊 测试 2: 首次内容绘制 (FCP)...
   ⏱️  FCP: 856ms ✅

📊 测试 3: DOM 内容加载...
   ⏱️  DOM 加载: 1234ms ✅

📊 测试 4: 资源分析...
   📦 资源数量: 42 ✅
   📦 总大小: 1.2 MB ✅

📊 测试 5: 内存使用...
   💾 JS 堆内存: 23.45 MB ✅

✅ 报告已保存到: reports/performance

📈 测试汇总:
   总测试数: 6
   通过: 6
   失败: 0
   通过率: 100.00%

✅ 所有性能测试通过！
```

---

## 4️⃣ 运行 k6 负载测试

### 步骤 4.1：安装 k6

**Windows (使用 Chocolatey)**
```bash
choco install k6
```

**Windows (使用 winget)**
```bash
winget install k6
```

**手动安装**
1. 访问 https://k6.io/docs/getting-started/installation/
2. 下载 Windows 安装包
3. 安装并添加到 PATH

### 步骤 4.2：验证安装

```bash
k6 version
```

### 步骤 4.3：确保开发服务器运行

```bash
npm run dev
```

### 步骤 4.4：运行 k6 测试

```bash
# 新终端
k6 run scripts\load-test.js
```

### 预期输出

```
          /\      |‾‾| /‾‾/   /‾‾/
     /\  /  \     |  |/  /   /  /
    /  \/    \    |     (   /   ‾‾\
   /          \   |  |\  \ |  (‾)  |
  / __________ \  |__| \__\ \_____/ .io

  execution: local
     script: scripts\load-test.js
     output: -

  scenarios: (100.00%) 1 scenario, 100 max VUs, 3m30s max duration
           * default: Up to 100 looping VUs for 3m0s over 5 stages

running (3m00.0s), 000/100 VUs, 1234 complete and 0 interrupted iterations
default ✓ [======================================] 000/100 VUs  3m0s

     ✓ 首页状态码为200
     ✓ 首页响应时间<500ms
     ✓ 登录页状态码为200
     ✓ 登录页响应时间<500ms

     checks.........................: 100.00% ✓ 4936      ✗ 0
     data_received..................: 12 MB   67 kB/s
     data_sent......................: 1.2 MB  6.7 kB/s
     http_req_duration..............: avg=234ms min=45ms med=198ms max=987ms p(95)=456ms
     http_reqs......................: 4936    27.4/s
     iterations.....................: 1234    6.9/s
     vus............................: 100     min=0       max=100
```

---

## 5️⃣ 设置并运行 SonarQube 分析

### 方案 A：使用 SonarCloud（推荐，免费）

#### 步骤 5A.1：注册 SonarCloud

1. 访问 https://sonarcloud.io/
2. 使用 GitHub 账号登录
3. 授权 SonarCloud 访问你的仓库

#### 步骤 5A.2：创建项目

1. 点击 "+" → "Analyze new project"
2. 选择你的 `campus-forum` 仓库
3. 点击 "Set Up"

#### 步骤 5A.3：获取 Token

1. 在 SonarCloud 项目页面，点击 "Administration" → "Analysis Method"
2. 选择 "Manually"
3. 生成并复制 Token

#### 步骤 5A.4：配置环境变量

```bash
# Windows PowerShell
$env:SONAR_HOST_URL="https://sonarcloud.io"
$env:SONAR_TOKEN="你的token"
$env:SONAR_ORGANIZATION="你的组织名"
```

#### 步骤 5A.5：更新 sonar-project.properties

```properties
sonar.projectKey=你的用户名_campus-forum
sonar.organization=你的组织名
sonar.host.url=https://sonarcloud.io
```

#### 步骤 5A.6：运行分析

```bash
npm run analyze:sonar
```

### 方案 B：使用本地 SonarQube

#### 步骤 5B.1：使用 Docker 运行 SonarQube

```bash
# 拉取镜像
docker pull sonarqube:community

# 运行容器
docker run -d --name sonarqube -p 9000:9000 sonarqube:community
```

#### 步骤 5B.2：访问 SonarQube

1. 打开 http://localhost:9000
2. 默认登录：admin / admin
3. 修改密码

#### 步骤 5B.3：创建项目

1. 点击 "Create Project"
2. 项目名称：campus-forum
3. 生成 Token

#### 步骤 5B.4：配置环境变量

```bash
$env:SONAR_HOST_URL="http://localhost:9000"
$env:SONAR_TOKEN="你的token"
```

#### 步骤 5B.5：运行分析

```bash
npm run analyze:sonar
```

### 预期输出

```
🔍 启动 SonarQube 代码分析...

INFO: Scanner configuration file: sonar-project.properties
INFO: Project root configuration file: NONE
INFO: SonarQube Scanner 4.8.0.2856
INFO: Java 11.0.17 Eclipse Adoptium (64-bit)
INFO: Analyzing on SonarQube 9.9.0
INFO: Default locale: "en_US", source code encoding: "UTF-8"
INFO: Load global settings
INFO: Load project settings
INFO: Load quality profiles
INFO: Load active rules
INFO: Indexing files...
INFO: 156 files indexed
INFO: Quality profile for js: Sonar way
INFO: Quality profile for ts: Sonar way
INFO: Sensor JavaScript/TypeScript analysis
INFO: Analysis report generated in 234ms
INFO: Analysis report uploaded in 567ms
INFO: ANALYSIS SUCCESSFUL

✅ SonarQube 分析完成！
📊 请访问 SonarQube 服务器查看详细报告
```

---

## 6️⃣ 运行完整的回归测试（包括 E2E）

### 步骤 6.1：安装 Playwright 浏览器

```bash
npx playwright install
```

### 步骤 6.2：启动开发服务器

```bash
# 终端 1
npm run dev
```

### 步骤 6.3：运行回归测试

```bash
# 终端 2：运行完整的回归测试
npm run test:regression
```

这会依次运行：
1. 所有单元测试
2. 所有 E2E 测试

### 步骤 6.4：查看 E2E 测试报告

```bash
# 如果测试失败，查看报告
npx playwright show-report
```

### 预期输出

```
> campus-forum@1.0.0 test:regression
> npm run test && npm run test:e2e

> campus-forum@1.0.0 test
> vitest

✓ tests/unit/stores/auth.test.ts (8)
✓ tests/unit/stores/posts.test.ts (12)
✓ tests/unit/utils/validation.test.ts (15)

Test Files  3 passed (3)
     Tests  35 passed (35)
  Start at  21:30:45
  Duration  2.34s

> campus-forum@1.0.0 test:e2e
> playwright test

Running 15 tests using 3 workers

  ✓  1 tests/e2e/auth.spec.ts:3:5 › 用户可以登录 (2s)
  ✓  2 tests/e2e/auth.spec.ts:15:5 › 用户可以注册 (3s)
  ✓  3 tests/e2e/home.spec.ts:3:5 › 首页正确加载 (1s)
  ✓  4 tests/e2e/navigation.spec.ts:3:5 › 导航栏工作正常 (2s)

  15 passed (12s)
```

---

## 🎯 完整演示流程

### 准备阶段（演示前一天）

```bash
# 1. 推送代码到 GitHub
git push origin main

# 2. 配置 SonarCloud
# 按照上面的步骤配置

# 3. 安装 k6
choco install k6

# 4. 安装 Playwright 浏览器
npx playwright install

# 5. 生成所有报告
npm run dev  # 终端1
npm run test:lighthouse  # 终端2
npm run test:performance  # 终端2
k6 run scripts\load-test.js  # 终端2
npm run analyze:sonar  # 终端2
npm run test:regression  # 终端2
```

### 演示当天

#### 演示 1：GitHub CI/CD
```bash
# 1. 打开 GitHub 仓库 Actions 页面
# 2. 修改文件
echo "# 演示" >> README.md
git add README.md
git commit -m "docs: 演示 CI 触发"
git push
# 3. 刷新 Actions 页面，展示自动运行
```

#### 演示 2：性能测试
```bash
# 终端1：启动服务器
npm run dev

# 终端2：运行测试
npm run test:lighthouse
npm run test:performance

# 展示报告
explorer reports\lighthouse
explorer reports\performance
```

#### 演示 3：k6 负载测试
```bash
# 确保服务器运行
k6 run scripts\load-test.js
```

#### 演示 4：SonarQube
```bash
# 运行分析
npm run analyze:sonar

# 打开 SonarCloud 或本地 SonarQube
# 展示代码质量仪表盘
```

#### 演示 5：回归测试
```bash
# 确保服务器运行
npm run test:regression

# 展示测试结果
```

---

## 📝 演示脚本

### 开场
"现在我将演示完整的测试与质量保证流程，包括 CI/CD、性能测试、代码分析等。"

### 演示 GitHub CI/CD
"首先是持续集成。我们的代码已经推送到 GitHub，配置了自动化流水线。现在我修改一个文件并提交..."
```bash
echo "# CI 演示" >> README.md
git add README.md
git commit -m "docs: 演示 CI"
git push
```
"可以看到 GitHub Actions 自动触发了，正在运行代码检查、测试和构建。"

### 演示性能测试
"接下来是性能测试。我们对比了三种工具。首先是 Lighthouse..."
```bash
npm run test:lighthouse
```
"Lighthouse 给出了性能评分和具体的优化建议。然后是自定义性能测试..."
```bash
npm run test:performance
```
"测试了加载时间、资源大小等指标。最后是 k6 负载测试..."
```bash
k6 run scripts\load-test.js
```
"模拟了 100 个并发用户，测试系统在高负载下的表现。"

### 演示代码分析
"现在运行 SonarQube 代码分析..."
```bash
npm run analyze:sonar
```
"SonarQube 会检测代码质量、安全漏洞、技术债务等。打开仪表盘可以看到详细的分析结果。"

### 演示回归测试
"最后是回归测试，运行所有单元测试和 E2E 测试..."
```bash
npm run test:regression
```
"所有测试都通过了，确保新代码没有破坏现有功能。"

---

## ✅ 验证清单

演示前确认：

- [ ] GitHub 仓库已创建并推送代码
- [ ] GitHub Actions 可以正常运行
- [ ] 开发服务器可以启动（`npm run dev`）
- [ ] Lighthouse 测试可以运行
- [ ] 自定义性能测试可以运行
- [ ] k6 已安装并可以运行
- [ ] SonarQube/SonarCloud 已配置
- [ ] Playwright 浏览器已安装
- [ ] E2E 测试可以运行
- [ ] 所有报告已生成并可以查看

---

## 🆘 故障排除

### 问题：开发服务器启动失败
```bash
# 清理并重新安装依赖
rm -rf node_modules package-lock.json
npm install
npm run dev
```

### 问题：Playwright 测试失败
```bash
# 重新安装浏览器
npx playwright install --with-deps
```

### 问题：k6 命令不存在
```bash
# 检查安装
k6 version

# 重新安装
choco install k6
```

### 问题：SonarQube 连接失败
```bash
# 检查环境变量
echo $env:SONAR_HOST_URL
echo $env:SONAR_TOKEN

# 重新设置
$env:SONAR_HOST_URL="你的URL"
$env:SONAR_TOKEN="你的token"
```

---

**准备好了吗？开始实现吧！** 🚀
