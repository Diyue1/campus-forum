# 📖 校园论坛系统 - 测试与质量保证操作指南

## 目录
- [快速开始](#快速开始)
- [环境准备](#环境准备)
- [测试操作](#测试操作)
- [代码质量检查](#代码质量检查)
- [性能测试](#性能测试)
- [测试跟踪与Bug管理](#测试跟踪与bug管理)
- [版本控制与Git Hooks](#版本控制与git-hooks)
- [持续集成](#持续集成)
- [常见问题](#常见问题)

---

## 快速开始

### 1. 安装依赖
```bash
npm install
```

### 2. 运行所有测试
```bash
# 单元测试
npm test

# 端到端测试（需先启动开发服务器）
npm run dev
# 新终端
npm run test:e2e
```

### 3. 代码质量检查
```bash
npm run lint
```

---

## 环境准备

### 必需软件
- **Node.js**: v18 或更高版本
- **npm**: v9 或更高版本
- **Git**: 用于版本控制

### 安装步骤
```bash
# 1. 克隆项目
git clone <仓库地址>
cd "Software Engineering Practice"

# 2. 安装依赖
npm install

# 3. 设置Git Hooks
node scripts/setup-git-hooks.js
```

---

## 测试操作

### 单元测试

#### 运行测试
```bash
# 运行所有单元测试
npm test

# 监听模式（开发时使用）
npm test -- --watch

# 运行特定测试文件
npm test -- src/stores/auth.test.ts
```

#### 查看测试覆盖率
```bash
# 生成覆盖率报告
npm run test:coverage

# 报告位置: coverage/index.html
```

#### 使用UI界面
```bash
npm run test:ui
# 浏览器会自动打开 http://localhost:51204
```

### 端到端测试

#### 运行E2E测试
```bash
# 1. 启动开发服务器
npm run dev

# 2. 新终端运行测试
npm run test:e2e

# 使用UI模式
npm run test:e2e:ui
```

#### 查看测试报告
```bash
# 测试失败时会自动生成报告
# 报告位置: playwright-report/index.html
npx playwright show-report
```

### 回归测试
```bash
# 运行所有测试（单元测试 + E2E测试）
npm run test:regression
```

---

## 代码质量检查

### ESLint 检查

#### 基本使用
```bash
# 运行代码检查并自动修复
npm run lint

# 只检查不修复
npx eslint src tests --ext .vue,.js,.ts
```

#### 安全检查
```bash
# 运行安全相关的代码检查
npm run lint:security
```

### SonarQube 分析

#### 前置条件
需要先启动SonarQube服务器或使用云服务。

#### 运行分析
```bash
# 设置环境变量
set SONAR_HOST_URL=http://localhost:9000
set SONAR_TOKEN=your_token_here

# 运行分析
npm run analyze:sonar
```

#### 查看结果
访问 SonarQube 服务器查看详细的代码质量报告。

---

## 性能测试

### Lighthouse 测试

#### 运行测试
```bash
# 1. 启动开发服务器
npm run dev

# 2. 新终端运行Lighthouse
npm run test:lighthouse
```

#### 查看报告
```bash
# 报告位置: reports/lighthouse/
# 打开最新的 HTML 报告文件
```

#### 测试指标说明
- **性能 (Performance)**: 页面加载速度和响应性
- **可访问性 (Accessibility)**: 无障碍访问支持
- **最佳实践 (Best Practices)**: Web开发最佳实践
- **SEO**: 搜索引擎优化

### 自定义性能测试

#### 运行测试
```bash
# 1. 启动开发服务器
npm run dev

# 2. 新终端运行性能测试
npm run test:performance
```

#### 查看报告
```bash
# 报告位置: reports/performance/
# 包含 JSON 和 HTML 两种格式
```

### k6 负载测试（可选）

#### 安装k6
```bash
# Windows (使用 Chocolatey)
choco install k6

# 或下载安装包
# https://k6.io/docs/getting-started/installation/
```

#### 创建测试脚本
在 `scripts/load-test.js` 中编写测试场景。

#### 运行测试
```bash
k6 run scripts/load-test.js
```

---

## 测试跟踪与Bug管理

### 记录测试结果

#### 手动记录
```bash
node scripts/test-tracker.js record
```

#### 自动记录
测试运行后会自动记录结果到系统中。

### Bug管理

#### 创建Bug
```bash
node scripts/test-tracker.js bug create "Bug标题" "详细描述" <严重程度>

# 严重程度: low, medium, high, critical
# 示例:
node scripts/test-tracker.js bug create "登录失败" "用户无法登录系统" high
```

#### 更新Bug状态
```bash
node scripts/test-tracker.js bug update <BUG-ID> <状态> "备注"

# 状态: open, in-progress, resolved, closed
# 示例:
node scripts/test-tracker.js bug update BUG-12345 resolved "已修复登录问题"
```

#### 查看Bug列表
```bash
node scripts/test-tracker.js bug list
```

### 生成报告

#### 生成测试报告
```bash
node scripts/test-tracker.js report
```

#### 查看统计信息
```bash
node scripts/test-tracker.js stats
```

#### 报告位置
- JSON报告: `reports/test-tracking/report-*.json`
- HTML报告: `reports/test-tracking/report-*.html`

---

## 版本控制与Git Hooks

### Git工作流

#### 分支策略
- `main`: 生产环境代码
- `develop`: 开发分支
- `feature/*`: 功能开发分支
- `hotfix/*`: 紧急修复分支

#### 创建功能分支
```bash
git checkout -b feature/new-feature
```

### Git Hooks

项目已配置以下Git钩子：

#### pre-commit（提交前）
自动执行：
- 代码格式化（Prettier）
- 代码检查（ESLint）
- 单元测试

#### commit-msg（提交信息）
验证提交信息格式，要求遵循约定式提交：
```bash
# 正确格式
git commit -m "feat: 添加用户登录功能"
git commit -m "fix: 修复登录页面样式问题"
git commit -m "docs: 更新README文档"

# 类型: feat, fix, docs, style, refactor, test, chore
```

#### pre-push（推送前）
运行完整测试套件和构建检查。

#### 跳过钩子（不推荐）
```bash
git commit --no-verify -m "message"
```

### 提交代码流程

```bash
# 1. 查看更改
git status

# 2. 添加文件
git add .

# 3. 提交（会触发pre-commit钩子）
git commit -m "feat: 添加新功能"

# 4. 推送（会触发pre-push钩子）
git push origin feature/new-feature
```

---

## 持续集成

### GitHub Actions

项目配置了自动化CI/CD流水线，在以下情况触发：
- 推送到 `main` 或 `develop` 分支
- 创建Pull Request

### CI流程

1. **代码检查**
   - ESLint代码质量检查
   
2. **单元测试**
   - 运行所有单元测试
   - 生成测试覆盖率报告
   
3. **端到端测试**
   - 运行E2E测试
   - 验证关键用户流程
   
4. **构建检查**
   - 验证生产构建

### 查看CI状态

1. 访问GitHub仓库
2. 点击 "Actions" 标签
3. 查看最近的工作流运行记录

### 本地模拟CI

```bash
# 运行与CI相同的检查
npm run lint
npm test
npm run test:e2e
npm run build
```

---

## 常见问题

### Q1: 测试失败怎么办？

**A:** 
1. 查看错误信息，定位问题
2. 检查相关代码是否按预期工作
3. 如果测试用例有问题，更新测试
4. 如果代码有问题，修复后重新运行测试

### Q2: 如何添加新的测试？

**A:**
1. 单元测试放在 `tests/unit/` 目录下
2. E2E测试放在 `tests/e2e/` 目录下
3. 测试文件命名规范：`*.test.ts` 或 `*.spec.ts`

### Q3: 如何提高测试覆盖率？

**A:**
1. 运行 `npm run test:coverage` 查看未覆盖的代码
2. 为未覆盖的函数和分支添加测试
3. 重点关注核心业务逻辑

### Q4: Git钩子失败怎么办？

**A:**
1. 查看错误信息
2. 修复代码质量问题或测试失败
3. 重新提交
4. 紧急情况可使用 `--no-verify` 跳过（不推荐）

### Q5: 性能测试失败怎么办？

**A:**
1. 查看具体哪个指标未达标
2. 优化相关代码（减少资源大小、优化加载速度等）
3. 重新运行测试验证

### Q6: SonarQube无法连接？

**A:**
1. 确保SonarQube服务器正在运行
2. 检查环境变量 `SONAR_HOST_URL` 和 `SONAR_TOKEN`
3. 可以先使用本地ESLint进行代码检查

### Q7: E2E测试超时？

**A:**
1. 确保开发服务器正在运行
2. 检查网络连接
3. 增加超时时间（在 `playwright.config.ts` 中配置）

---

## 最佳实践

### 开发流程
1. 创建功能分支
2. 编写代码和测试
3. 本地运行测试确保通过
4. 提交代码（触发Git钩子）
5. 推送到远程仓库
6. 创建Pull Request
7. 等待CI通过
8. 代码审查
9. 合并到主分支

### 测试编写
- 遵循AAA模式：Arrange（准备）、Act（执行）、Assert（断言）
- 测试应该独立、可重复
- 使用描述性的测试名称
- 覆盖正常情况和边界情况

### 代码质量
- 提交前运行 `npm run lint`
- 保持代码简洁、可读
- 添加必要的注释
- 遵循项目代码规范

---

## 获取帮助

- **项目文档**: [README.md](README.md)
- **测试指南**: [TESTING-AND-QUALITY.md](TESTING-AND-QUALITY.md)
- **问题反馈**: 创建GitHub Issue

---

## 附录：命令速查表

| 命令 | 说明 |
|------|------|
| `npm install` | 安装依赖 |
| `npm run dev` | 启动开发服务器 |
| `npm test` | 运行单元测试 |
| `npm run test:coverage` | 生成测试覆盖率报告 |
| `npm run test:e2e` | 运行端到端测试 |
| `npm run test:performance` | 运行性能测试 |
| `npm run test:lighthouse` | 运行Lighthouse测试 |
| `npm run test:regression` | 运行回归测试 |
| `npm run lint` | 代码质量检查 |
| `npm run lint:security` | 安全检查 |
| `npm run analyze:sonar` | SonarQube分析 |
| `npm run build` | 构建生产版本 |
| `npm run preview` | 预览生产版本 |
| `node scripts/test-tracker.js report` | 生成测试报告 |
| `node scripts/setup-git-hooks.js` | 设置Git钩子 |

---

**最后更新**: 2025年12月18日
